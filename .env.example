# At least one key is needed for the AI features.
# Users can also bring their own key via Profile > AI Settings.

# Anthropic (Claude) — used by default
ANTHROPIC_API_KEY=sk-ant-your-key-here

# OpenAI (GPT) — optional alternative
OPENAI_API_KEY=sk-your-key-here
